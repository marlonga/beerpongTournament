import javax.swing.*;
import java.awt.*;

public class RankingPanel extends JPanel {
    public RankingPanel () {
        setSize(new Dimension(200,500));
        setLocation(630,10);
        setLayout(null);
        setBackground( new Color(146,252,134));
        updateUI();
    }
}
